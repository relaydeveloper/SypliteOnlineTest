const template = {
  name: 'Test',
  title: 'Save'
}
const htmlTemplate = `
 <div class="template">
    <h2>
        ${template.name}
    </h2>
    <button type="button">${template.title}</button>
 </div>
`;